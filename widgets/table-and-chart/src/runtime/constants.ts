export enum PlotType {
  TIME_SERIES = 0,
  SCATTER = 1,
}

export enum PlotMode {
  MARKERS = "markers",
  LINES_AND_MARKERS = "lines+markers",
}

export enum ChartDisplayMode {
  SCATTER = "scatter",
  TABLE = "table",
}
